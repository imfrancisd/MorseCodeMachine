#include "convertMorseFromGreek.h"
#include <stddef.h>

int convertMorseFromGreek(const char greekMessage[], char englishBuffer[], size_t englishBufferSize)
{
    if (!(greekMessage && englishBuffer))
    {
        return -1;
    }

    return 0;
}

